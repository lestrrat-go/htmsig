# htmsig - RFC9421 HTTP Message Signatures for Go (WIP)

* Sign/Verify kinda works.
* Factor out sfv
* Need to refactor out signing/verification into its own package (currently uses github.com/lestrrat-go/jwx)